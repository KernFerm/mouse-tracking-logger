# Configuration settings for mouse tracking

# Set the logging interval (in seconds) - controls how often the mouse position is logged
LOG_INTERVAL = 0.03  # Faster interval for smoother tracking (adjust as needed)

# Path for the log file where mouse positions will be saved
LOG_FILE_PATH = "mouse_tracking_log.txt"

# Batch size - how many entries to buffer before writing to the file
LOG_BATCH_SIZE = 20
